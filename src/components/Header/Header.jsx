import * as React from "react";
import "./Header.scss";

/**
 * Header component.
 *
 * @returns {*}
 * @constructor
 */
const Header = ({}) => (
  <header className="header">
    <h1>Header</h1>
  </header>
);

/** PropTypes */
Header.propTypes = {};

export default Header;
