import * as React from "react";
import "./Header.scss";

/**
 * Header component.
 *
 * @returns {*}
 * @constructor
 */
const Header = ({}) => (
  <header className="header">
    Header
  </header>
);

/** PropTypes */
Header.propTypes = {};

export default Header;
