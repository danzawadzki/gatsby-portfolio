import * as React from "react";

/**
 * Slider component.
 *
 * @returns {*}
 * @constructor
 */
const Slider = ({}) => <h1>Slider</h1>;

/** PropTypes */
Slider.propTypes = {};

export default Slider;
